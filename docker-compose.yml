services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: athena-server
    ports:
      - "3001:3000"
    environment:
      SERVER_PORT: 3000
      SERVER_HOST: 0.0.0.0
      DATABASE_URL: "file:/app/data/athena.db"
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-me}
      SEED_USERNAME: ${SEED_USERNAME:-admin}
      SEED_PASSWORD: ${SEED_PASSWORD:-admin}
      SPOTIFY_CLIENT_ID: ${SPOTIFY_CLIENT_ID}
      SPOTIFY_CLIENT_SECRET: ${SPOTIFY_CLIENT_SECRET}
      SPOTIFY_REFRESH_TOKEN: ${SPOTIFY_REFRESH_TOKEN}
      SPOTIFY_REDIRECT_URI: ${SPOTIFY_REDIRECT_URI:-http://localhost:5173/auth/spotify/callback}
      # Athena code execution sandbox (run_code tool). Requires Docker socket
      # mounted below. Set to "false" to disable code execution.
      SANDBOX_ENABLED: ${SANDBOX_ENABLED:-true}
    volumes:
      - athena-data:/app/data
      - athena-uploads:/app/uploads
      # Docker socket for the Athena code execution sandbox (run_code tool).
      # Mount only on trusted single-user deployments. Set SANDBOX_ENABLED=false
      # to disable code execution without removing this mount.
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: athena-client
    ports:
      - "5173:80"
    environment:
      VITE_API_URL: http://localhost:3001
    depends_on:
      - server
    restart: unless-stopped

volumes:
  athena-data:
  athena-uploads:
